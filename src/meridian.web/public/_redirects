# Static pages must resolve to themselves before the SPA catch-all below,
# otherwise the client-side router swallows them and serves index.html.
/team.html    /team.html    200

# Everything else is handled by the React router.
/*            /index.html   200
